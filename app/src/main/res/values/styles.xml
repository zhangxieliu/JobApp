<resources>

    <style name="AppTheme" parent="Base.Widget.AppCompat.ActionBar">
        <item name="android:windowActionBarOverlay">true</item>
        <!-- 让actionbar漂浮 -->
        <item name="android:actionBarStyle">@style/my_actionbar_style</item>
    </style>

    <style name="my_actionbar_style" parent="Base.Widget.AppCompat.ActionBar">
        <!-- 设置actionbar 背景色 透明 -->
        <item name="android:background">#33000000</item>
    </style>

    <style name="bottom_menu_item_layout">
        <item name="android:layout_width">0dp</item>
        <item name="android:layout_height">match_parent</item>
        <item name="android:layout_weight">1</item>
        <item name="android:orientation">vertical</item>
        <item name="android:gravity">center</item>
    </style>
    <style name="bottom_menu_item_icon">
        <item name="android:layout_width">30dp</item>
        <item name="android:layout_height">30dp</item>
    </style>
    <style name="bottom_menu_item_text">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">match_parent</item>
        <item name="android:textSize">12sp</item>
        <item name="android:textColor">#353333</item>
        <item name="android:gravity">center</item>
    </style>
</resources>
